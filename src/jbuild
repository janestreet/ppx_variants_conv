(library
 ((name ppx_variants_conv)
  (public_name ppx_variants_conv)
  (kind ppx_type_conv_plugin)
  (ppx_runtime_libraries (variantslib))
  (libraries (compiler-libs.common ppx_core ppx_driver ppx_type_conv))
  (preprocess metaquot)))
